# React 19 + older Radix UI packages declare React ^18 peer ranges.
# legacy-peer-deps lets installs resolve cleanly (matches the committed lockfile)
# and ensures Vercel's `npm install` does not fail with ERESOLVE.
legacy-peer-deps=true
